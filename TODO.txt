Fetcher
    Drission Page Fetcher
        browser.wait.doc_loaded() redundant once
        config sleep times
        need to be able to return markdown or html with screenshot
    Get page (takes url, returns html or markdown and screenshot option)
    implement proxy server use
    get permission from venues to crawl and do some crawling through a proxy firecrawl and some through a stable permissioned ip
    venue config to do certain amount of waiting or other actions on pages
        Try to load more results by scrolling or finding load more button
    Add HttpInterface so that we can pass a mock into the fetcher to test but use the same interface as the package used now
    Accept 301 redirects
    Sometimes 404 test does not work for drission page scraper because it gets 200s back somehow
    Create FirecrawlFetcher
Config Loader
    Need testing for overrides
Image Saver
    S3 Image Saver
DataExtractor
    Extract all schema and return it as well
    Classification algo extractor to replace LLM extractors
Scraper
    Create Scraper that takes FetcherFactory and DataExtractorFactory
    pass scraper factory into event data manager and event data manager will get the appropriate scraper for the job
    pass data extractor factory to scraper and scraper decides which extractor to use for the job based on scraper config
Event Data Manager
    Merge events function
    Skip data extraction if hash from previous scrape matches
    Submit updates to a separate table to be reviewed before they are committed to events table
    Make sure to use schema if fetcher returns any and skip extraction if there is sufficient data or include extraction as well and have both in suggested_transaction entry
MySQL
    Create local mysql to connect to for local env testing
    create new events table with new schema
        last crawl
        event page url, hash, screenshot, highlighted screenshot, html
        event list page url, hash, screenshot, highlighted screenshot, html
        status: pending-review, pending-update, published
        performances; one for each performer with start and end time and link to artists db
    transaction_suggestion table
        table (events)
        current data row id (if update or delete)
        suggested data (maybe this is a row in the actual data table that has a new id and a disabled status)
        transaction type (create, update, delete)
        data index (order this data appeared on the page if this is not the only data found on the page)
        screenshot(s)
        schema
    logs table
        timestamp
        application
        log type
        message
Tests
    Classes to test
        Event
        EventList
        Fetcher
        FirecrawlFetcher
        OpenAiDataExtractor
        Scraper
        MySqlInterface
        FirecrawlScraper
        EventDataManager
    Test getting data from event url
    Consistency testing with multiple runs on same test run to check consistency level as a percentage
Deploy
    use/install python3.11 on ec2
Manual review app
    create rest api to serve up suggested transactions and accept transaction approvals to update the RDS
    expose rest api service publicly from ec2
    create web app for reviewing and approving or rejecting or modifying each suggestion (that way i can get a VA to do it at some point)
        show screenshots, event schema, and other raw data on the left and the suggestion on the right along with any current state of the data
        you can edit the suggested data
        button for approve, reject; approve changes to approve with changes when changes made, and revert changes which is disabled until change is made
        button for reject and re-scrape
        differences between current and suggested data are highlighted
        log the approvals, rejections, and approvals with changes so that i can see which venues have issues and manually fix or improve accuracy with venue configs
Logging
    create Logger class and it should be able to print and add logs to the db
    configure logger with the application and other constants
    keep track of 404s, ip blocked, 301s, any other failures
    create application that allows me to easily see alerts for issues that come up daily
    purge logs after certain time (maybe 3 months)
Instagram
    Get posts for the venue and detect if they have event data or not using classification algo
    posts with event data get analyzed for data extraction and transaction_suggestions are created
