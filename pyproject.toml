[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "agro"
version = "0.1.0"
description = "Event Aggregator CLI"
requires-python = ">=3.11"
dependencies = [
    "pytest>=7.0",
    "pymysql>=1.1",
    "google-genai>=0.3.0",
]
